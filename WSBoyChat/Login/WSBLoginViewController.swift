//
//  WSBLoginViewController.swift
//  WSBoyChat
//
//  Created by hezhichang on 2020/8/4.
//  Copyright © 2020 zhichang.he. All rights reserved.
//

import UIKit

class WSBLoginViewController: UIViewController {
    
    var titleLabel = UILabel(frame: .zero)
    var emailTitleLabel = UILabel(frame: .zero)
    var emailInputView = UIInputView(frame: .zero, inputViewStyle: .default)
    var emailTipsLabel = UILabel(frame: .zero)
    var password
    
    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
